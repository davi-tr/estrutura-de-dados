#include <iostream>
#include <Stdlib.h>
#include "backoffice.h"

int* criavet(int dim) {
	int* tmp = (int*)malloc(dim * sizeof(int));
	return tmp;
}

int* limpavet(int dim) {
	int* tmp = (void* ptr, size_t int);
	return tmp;
}